# Database
DB_HOST=localhost
DB_PORT=5432
DB_NAME=pyga
DB_USER=postgres
DB_PASSWORD=change-me

# Auth
JWT_SECRET=change-me
ADMIN_EMAIL=admin@example.com

# Optional CORS origins, comma separated
ALLOWED_ORIGINS=

# Game cover images (server-side only; never put the key in Flutter)
RAWG_API_KEY=
